# Rest Todo App using node with express

// Vanilla JS
//itemNode.children().filter('.delete_links').click(getDeleteClickHandler(itemNode))

Siehe Files zum Test der Schnittstellen (Insomnia, Python) im resources-Folder
